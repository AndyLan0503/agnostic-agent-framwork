---
reconcile:
  direction: code-is-truth
  bindings:
    - doc_anchor: scaled-behavior
      governs: calc.py
      code_anchor: "def scaled_add"
---

# Scaled

<!-- reconcile:scaled-behavior:start -->
`scaled_add(a, b, factor)` returns `add(a, b)` multiplied by `factor`.
<!-- reconcile:scaled-behavior:end -->
