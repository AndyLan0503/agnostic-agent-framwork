All durable guidance for this repository lives in the root AGENTS.md - read
it before making changes (docs/adr/0001). Fast reference: facts in
`knowledge/`, decisions in `docs/adr/`, roles in `roles/`, runbooks in
`skills/`.
